// import { Router } from "express";
// import * as generalCandidateController from "@/controllers/generalCandidateController";

// const router = Router();

// // Candidate CRUD routes
// router.get("/", generalCandidateController.getCandidates);
// router.get("/:id", generalCandidateController.getCandidateById);
// router.post("/", generalCandidateController.createCandidate);
// router.put("/:id", generalCandidateController.updateCandidate);
// router.delete("/:id", generalCandidateController.deleteCandidate);

// export default router;
